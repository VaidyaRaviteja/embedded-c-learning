#include <stdio.h>

int main(void)
{
    const int MAX_USERS = 100;
    const float PI = 3.14159f;
    const char GRADE = 'A';

    printf("Maximum users: %d\n", MAX_USERS);
    printf("PI: %.5f\n", PI);
    printf("Grade: %c\n", GRADE);

    return 0;
}